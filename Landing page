<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
<title>BYINFORGH — Get Your Business Seen (LIVE DEMO)</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Syne:wght@600;700;800&family=Space+Mono:wght@400;700&display=swap');
  :root{
    --bg:#0a0a0a; --panel:#141210; --gold:#f5c842; --gold-dim:#c9a233; --teal:#3fb6a8;
    --cream:#faf6ec; --text:#f2ede0; --muted:#a89f8c; --line:#2a271f; --radius:18px;
  }
  *{box-sizing:border-box;} html,body{margin:0;padding:0;}
  body{
    background: radial-gradient(circle at 15% 0%, rgba(245,200,66,0.10), transparent 45%),
      radial-gradient(circle at 90% 100%, rgba(63,182,168,0.10), transparent 45%), var(--bg);
    color:var(--text); font-family:'Space Mono', monospace; min-height:100vh;
    display:flex; align-items:center; justify-content:center; padding:20px;
  }
  .demo-flag{position:fixed;top:0;left:0;right:0;background:var(--teal);color:#0a0a0a;text-align:center;
    font-family:'Syne',sans-serif;font-weight:800;font-size:12px;letter-spacing:2px;padding:6px;z-index:999}
  .card{width:100%;max-width:480px;background:var(--panel);border:1px solid var(--line);border-radius:var(--radius);
    padding:44px 24px 24px;position:relative;overflow:hidden;margin-top:26px}
  .card::before{content:"";position:absolute;top:0;left:0;right:0;height:4px;background:linear-gradient(90deg,var(--gold),var(--teal))}
  .brand{display:flex;align-items:center;gap:10px;margin-bottom:22px}
  .brand .mark{width:34px;height:34px;border-radius:8px;background:var(--gold);color:#12100c;display:flex;
    align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-weight:800;font-size:18px}
  .brand .name{font-family:'Syne',sans-serif;font-weight:700;font-size:16px;letter-spacing:0.5px}
  .brand .name b{color:var(--gold)}
  .brand .tag{font-size:10px;color:var(--muted);letter-spacing:1px;text-transform:uppercase}
  .progress{display:flex;gap:6px;margin-bottom:22px}
  .progress .seg{flex:1;height:4px;border-radius:2px;background:var(--line);transition:background .3s}
  .progress .seg.done{background:var(--gold)}
  h1{font-family:'Syne',sans-serif;font-weight:800;font-size:22px;line-height:1.25;margin:0 0 6px;color:var(--cream)}
  p.sub{color:var(--muted);font-size:13px;line-height:1.5;margin:0 0 20px}
  label{display:block;font-size:11px;text-transform:uppercase;letter-spacing:1px;color:var(--gold);margin:16px 0 6px}
  label.req::after{content:" *";color:var(--teal)}
  input[type=text],input[type=tel],select,textarea{width:100%;background:#1c1a15;border:1px solid var(--line);
    color:var(--text);font-family:'Space Mono',monospace;font-size:15px;padding:14px;border-radius:10px;outline:none}
  input:focus,select:focus,textarea:focus{border-color:var(--gold)}
  textarea{resize:vertical;min-height:70px}
  .choice{display:block;width:100%;text-align:left;background:#1c1a15;border:1px solid var(--line);color:var(--text);
    font-family:'Space Mono',monospace;font-size:15px;line-height:1.4;padding:16px;border-radius:12px;margin-bottom:12px;
    cursor:pointer;transition:border-color .15s,background .15s}
  .choice b{color:var(--gold);font-family:'Syne',sans-serif}
  .choice:active,.choice.tapped{border-color:var(--gold);background:#221f16}
  .pkg-choice{display:block;width:100%;text-align:left;background:#1c1a15;border:1px solid var(--line);color:var(--text);
    font-family:'Space Mono',monospace;padding:14px 16px;border-radius:12px;margin-bottom:10px;cursor:pointer;
    transition:border-color .15s,background .15s}
  .pkg-choice:active{border-color:var(--gold);background:#221f16}
  .pkg-name{font-family:'Syne',sans-serif;font-weight:700;font-size:15px;color:var(--cream)}
  .pkg-price{color:var(--gold);font-weight:700;font-size:13px;margin:2px 0 6px}
  .pkg-blurb{font-size:12px;color:var(--muted);line-height:1.4}
  .btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;width:100%;padding:16px;border-radius:12px;
    border:none;font-family:'Syne',sans-serif;font-weight:700;font-size:15px;cursor:pointer;margin-top:8px}
  .btn-primary{background:var(--gold);color:#141210}
  .btn-primary:disabled{opacity:.4;cursor:not-allowed}
  .btn-ghost{background:transparent;color:var(--muted);border:1px solid var(--line)}
  .btn-teal{background:var(--teal);color:#0a0a0a}
  .row{display:flex;gap:10px;margin-top:14px}
  .row .btn{margin-top:0}
  .step{display:none} .step.active{display:block;animation:fade .25s ease}
  @keyframes fade{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:translateY(0)}}
  .success-icon{width:56px;height:56px;border-radius:50%;background:rgba(63,182,168,0.15);border:1px solid var(--teal);
    display:flex;align-items:center;justify-content:center;font-size:26px;margin-bottom:14px}
  .muted-note{font-size:12px;color:var(--muted);margin-top:14px;line-height:1.5}
  .err{color:#e77;font-size:12px;margin-top:6px;min-height:16px}
  .spinner{width:16px;height:16px;border-radius:50%;border:2px solid rgba(20,18,16,0.3);border-top-color:#141210;
    animation:spin .7s linear infinite;display:none}
  @keyframes spin{to{transform:rotate(360deg)}}
  .live-note{background:#0a1a18;border:1px solid var(--teal);border-radius:8px;padding:10px 12px;font-size:11px;
    color:var(--teal);margin-bottom:16px;line-height:1.5}
</style>
</head>
<body>
<div class="demo-flag">⚡ LIVE DEMO — THE DIGITAL PRESENCE &amp; CUSTOMER SYSTEM © BYINFORGH — this writes to Google Sheets and the Command Centre in real time</div>

<div class="card">
  <div class="brand">
    <div class="mark">B</div>
    <div><div class="name">BY<b>INFORGH</b></div><div class="tag">Visible · Trusted · Local</div></div>
  </div>
  <div class="progress" id="progress"></div>

  <form id="leadForm" autocomplete="off">
    <input class="hp" type="text" name="website" id="website" tabindex="-1" autocomplete="off" style="position:absolute;left:-9999px">

    <div class="step active" data-step="1">
      <h1>Let's get your business found.</h1>
      <p class="sub">Takes under a minute. No login needed.</p>
      <label class="req">Business name</label><input type="text" id="business" required>
      <label class="req">WhatsApp number</label><input type="tel" id="number" placeholder="024 123 4567" required>
      <label class="req">Town</label><input type="text" id="town" required>
      <label class="req">Category — what do you sell or do?</label><input type="text" id="category" placeholder="e.g. fashion, food, salon" required>
      <label class="req">Your name</label><input type="text" id="contactName" required>
      <div class="err" id="err1"></div>
      <button type="button" class="btn btn-primary" onclick="goStep(2)">Next</button>
    </div>

    <div class="step" data-step="2">
      <span style="display:inline-block;background:rgba(245,200,66,0.12);color:var(--gold);border:1px solid rgba(245,200,66,0.35);font-size:11px;padding:4px 10px;border-radius:20px;margin-bottom:12px">Quick question</span>
      <h1>Which stage best describes your business today?</h1>
      <button type="button" class="choice" data-field="stage" data-value="found" onclick="pick(this,'stage','found',3)"><b>Found</b> — I need more people to find my business.</button>
      <button type="button" class="choice" data-field="stage" data-value="customers" onclick="pick(this,'stage','customers',3)"><b>Customers</b> — I need more customers.</button>
      <button type="button" class="choice" data-field="stage" data-value="systems" onclick="state.approach='expert';pick(this,'stage','systems','3b')"><b>Systems</b> — I need better systems to manage my business.</button>
      <button type="button" class="btn btn-ghost" onclick="goStep(1)">Back</button>
    </div>

    <div class="step" data-step="3">
      <span style="display:inline-block;background:rgba(245,200,66,0.12);color:var(--gold);border:1px solid rgba(245,200,66,0.35);font-size:11px;padding:4px 10px;border-radius:20px;margin-bottom:12px">How do you want to fix this?</span>
      <h1>Pick your path.</h1>
      <button type="button" class="choice" data-field="approach" data-value="diy" onclick="pick(this,'approach','diy',4)"><b>A) Do it myself</b> — send me the free blueprint.</button>
      <button type="button" class="choice" data-field="approach" data-value="expert" onclick="pick(this,'approach','expert','3b')"><b>B) Expert help</b> — I want a team to build or manage it for me.</button>
      <button type="button" class="btn btn-ghost" onclick="goStep(2)">Back</button>
    </div>

    <div class="step" data-step="3b">
      <span style="display:inline-block;background:rgba(245,200,66,0.12);color:var(--gold);border:1px solid rgba(245,200,66,0.35);font-size:11px;padding:4px 10px;border-radius:20px;margin-bottom:12px">Expert help</span>
      <h1>Pick the package that fits.</h1>
      <p class="sub">Tap one to lock it in.</p>
      <div id="packageChoices"></div>
      <button type="button" class="btn btn-ghost" onclick="goStep(state.stage==='systems'?2:3)">Back</button>
    </div>

    <div class="step" data-step="4">
      <h1 id="confirmHeadline">Ready to send?</h1>
      <p class="sub" id="confirmSub"></p>
      <div class="live-note">⚡ Tapping submit writes this straight into the demo Google Sheet — the Command Centre dashboard will pick it up on its next refresh, live, in front of your client.</div>
      <button type="button" class="btn btn-primary" id="submitBtn" onclick="submitLead()">
        <span class="spinner" id="spinner"></span><span id="submitLabel">Submit</span>
      </button>
      <div class="err" id="err5"></div>
      <button type="button" class="btn btn-ghost" onclick="goStep(state.approach==='expert'?'3b':3)">Back</button>
    </div>

    <div class="step" data-step="5"><div id="outcome"></div></div>
  </form>
</div>

<script>
// ============================================================
// PASTE YOUR APPS SCRIPT WEB APP URL HERE (ends in /exec)
// Deploy Code.gs first — see setup instructions at the top of that file.
// ============================================================
const CONFIG = {
  GAS_URL: "https://script.google.com/macros/s/AKfycbwNsvuFocgeZR8x029VYkmWydE8py3ceSGxfqpnk_7gkkN_mndCvGnluLSmOcKBIkjeVQ/exec",
  WHATSAPP_NUMBER: "233509960980",
  PACKAGES: {
    general: [
      {key:'verified',name:'Verified Business',price:'GHS 180 setup + GHS 80/mo',blurb:'Build trust. Get your blue tick. Get found.',crmOffer:'Verified Business — 180 GHS setup + 80 GHS/month'},
      {key:'essentials',name:'Essential Business',price:'GHS 250/month',blurb:'Everything you need to be seen — guidance, systems, checklists, training.',crmOffer:'Visibility Essential 250'},
      {key:'partner',name:'Visibility Partner Starter',price:'GHS 750/month',blurb:'We guide you, you grow — done-with-you support.',crmOffer:'Visibility Partner 750'},
      {key:'growth',name:'Growth Engine',price:'GHS 1,500/month',blurb:'We build. We manage. You grow.',crmOffer:'Growth Partner 1500'},
      {key:'authority',name:'Authority Partner',price:'GHS 5,000/month',blurb:'We build. We run. You dominate.',crmOffer:'Authority Market 5000'}
    ],
    systems: [
      {key:'sme_starter',name:'SME Starter',price:'GHS 50/month',blurb:'Simple systems, strong foundation.',crmOffer:'SME Starter 50'},
      {key:'sme_growth',name:'SME Growth',price:'GHS 150/month',blurb:'SOPs, setup guide, training materials.',crmOffer:'SME Growth 150'},
      {key:'sme_authority',name:'SME Authority',price:'GHS 500/month',blurb:'Ongoing support, accountability, advanced growth.',crmOffer:'SME Authority 500'}
    ]
  }
};
const state = { stage:null, approach:null, package:null };
const totalSteps = 4;

function buildProgress(){const el=document.getElementById('progress');el.innerHTML='';for(let i=1;i<=totalSteps;i++){const seg=document.createElement('div');seg.className='seg';seg.id='seg'+i;el.appendChild(seg);}}
buildProgress();
function updateProgress(step){const stepNum={1:1,2:2,3:3,'3b':3,4:4}[step]||step;for(let i=1;i<=totalSteps;i++)document.getElementById('seg'+i).classList.toggle('done',i<=stepNum);}

function validateStep1(){
  const business=document.getElementById('business').value.trim();
  const number=document.getElementById('number').value.trim();
  const town=document.getElementById('town').value.trim();
  const category=document.getElementById('category').value.trim();
  const contactName=document.getElementById('contactName').value.trim();
  const hasLetters=v=>/[a-zA-Z]{2,}/.test(v);
  const digits=number.replace(/[\s-]/g,'');
  const validPhone=/^0\d{9}$/.test(digits)||/^(\+233|233)\d{9}$/.test(digits);
  if(business.length<2||!hasLetters(business))return'Enter your full business name.';
  if(!validPhone)return'Enter a valid Ghana WhatsApp number, e.g. 024 123 4567.';
  if(town.length<2||!hasLetters(town))return'Enter your town.';
  if(category.length<2||!hasLetters(category))return'Tell us what you sell or do.';
  if(contactName.length<2||!hasLetters(contactName))return'Enter your name.';
  return '';
}
function goStep(n){
  if(n===2){const errMsg=validateStep1();if(errMsg){document.getElementById('err1').textContent=errMsg;return;}document.getElementById('err1').textContent='';}
  document.querySelectorAll('.step').forEach(s=>s.classList.remove('active'));
  document.querySelector('.step[data-step="'+n+'"]').classList.add('active');
  updateProgress(n);
  if(n==='3b')renderPackageStep();
  if(n===4)setConfirmCopy();
}
function renderPackageStep(){
  const group=state.stage==='systems'?CONFIG.PACKAGES.systems:CONFIG.PACKAGES.general;
  document.getElementById('packageChoices').innerHTML=group.map(pkg=>`
    <button type="button" class="pkg-choice" onclick='selectPackage(${JSON.stringify(pkg)})'>
      <div class="pkg-name">${pkg.name}</div><div class="pkg-price">${pkg.price}</div><div class="pkg-blurb">${pkg.blurb}</div>
    </button>`).join('');
}
function selectPackage(pkg){state.package=pkg;goStep(4);}
function pick(btn,field,value,nextStep){
  document.querySelectorAll('.choice[data-field="'+field+'"]').forEach(b=>b.classList.remove('tapped'));
  btn.classList.add('tapped'); state[field]=value;
  setTimeout(()=>goStep(nextStep),150);
}
function setConfirmCopy(){
  const stageLabel={found:'getting found',customers:'getting more customers',systems:'better systems'}[state.stage];
  if(state.approach==='expert'&&state.package){
    document.getElementById('confirmHeadline').textContent=state.package.name+' — '+state.package.price;
    document.getElementById('confirmSub').textContent='Tap submit — this lead goes straight into the Command Centre.';
  }else{
    document.getElementById('confirmHeadline').textContent='You want help with '+stageLabel+'.';
    document.getElementById('confirmSub').textContent='Tap submit — this lead goes straight into the Command Centre.';
  }
}
async function submitLead(){
  const errEl=document.getElementById('err5'); errEl.textContent='';
  const btn=document.getElementById('submitBtn'), spinner=document.getElementById('spinner'), label=document.getElementById('submitLabel');
  btn.disabled=true; spinner.style.display='inline-block'; label.textContent='Submitting...';
  const payload={
    action:'submitLandingLead',
    business:document.getElementById('business').value.trim(),
    number:document.getElementById('number').value.trim(),
    town:document.getElementById('town').value.trim(),
    category:document.getElementById('category').value.trim(),
    contactName:document.getElementById('contactName').value.trim(),
    source:'Demo Landing Page',
    quizStage:state.stage, quizApproach:state.approach
  };
  if(state.approach==='expert'&&state.package){ payload.offer=state.package.crmOffer; payload.offerPrice=state.package.price; }
  const qs=Object.entries(payload).map(([k,v])=>k+'='+encodeURIComponent(v)).join('&');
  let writeSucceeded=false;
  try{
    const res=await fetch(CONFIG.GAS_URL+'?'+qs);
    const data=await res.json();
    if(!data.success)throw new Error(data.error||'Unknown error');
    writeSucceeded=true;
  }catch(e){ console.warn('Demo submit issue:',e); }
  finally{
    btn.disabled=false; spinner.style.display='none'; label.textContent='Submit';
    showOutcome(writeSucceeded);
  }
}
function showOutcome(writeSucceeded){
  const el=document.getElementById('outcome');
  const pkg=state.package;
  el.innerHTML=`
    <div class="success-icon">✓</div>
    <h1>You're logged. Check the Command Centre now.</h1>
    <p class="sub">${pkg?pkg.name+' — <b style="color:var(--gold)">'+pkg.price+'</b>':'Your request is in the pipeline.'}</p>
    ${writeSucceeded?'<div class="live-note">✓ Written to the Google Sheet just now. Refresh the Command Centre — this contact will be sitting right at the top.</div>':'<div class="live-note" style="border-color:#e77;color:#e77">Could not confirm the write — check the GAS_URL is set correctly and the Apps Script is deployed.</div>'}
    <a class="btn btn-teal" style="text-decoration:none;color:#0a0a0a;display:block;text-align:center;margin-top:10px" href="https://wa.me/${CONFIG.WHATSAPP_NUMBER}" target="_blank" rel="noopener">Continue on WhatsApp</a>
  `;
  document.querySelectorAll('.step').forEach(s=>s.classList.remove('active'));
  document.querySelector('.step[data-step="5"]').classList.add('active');
}
</script>
</body>
</html>
